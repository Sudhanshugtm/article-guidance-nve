// ABOUTME: Locale registry — exports all UI locale bundles keyed by language code.
// ABOUTME: Statically imported to avoid async loading flicker.

import en from './en.js'
import tr from './tr.js'
import pt from './pt.js'

export default { en, tr, pt }
